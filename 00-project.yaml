title: "SYNAPSE — Coordination as Compression (协作即压缩)"
direction: "面向多智能体协作的低开销通信、状态传递与共享记忆机制（社区赛题）；兼投 CCF-A 主会"
entry_route: E3            # 已有 idea + 设计草案（SYNAPSE.tex 含 E1–E8 实验雏形）→ S2 校验/补缺模式
target_venues:
  - venue: 社区赛题（多智能体协作低开销通信/状态传递/共享记忆）
    deadline: ""           # source: assumed — 官方 deadline 未登记，进决策包待用户确认
    policy_risk: "none-found-2026-06-18；交付须 openEuler 24.03-LTS-SP3 可编译运行测试"
  - venue: CCF-A（NeurIPS/ICML/ICLR/ACL 档）
    deadline: ""           # 视收缩律实验成熟度定，estimated
    policy_risk: "需与最近邻 arXiv:2604.09521 正面区分（静态语义率失真）"
constraints:
  compute:
    value: "无独立大显存 GPU 基线；LLM 走 SiliconFlow API（Qwen2.5-7B/14B-Instruct）；非文本载体=本地 sentence-embedding 预测残差；向量库 FAISS/Milvus-lite 本地小规模；隐状态零拷贝路径为需本地 vLLM 的可选臂"
    source: user           # 2026-06-18 用户选定 "API+本地向量(无需GPU)"
  data:
    value: "自建关联任务族 G1/G2（多跳研究报告类）+ 本地文档语料；无需受限数据集"
    source: assumed        # 任务族语料待 S2 细化，进决策包
  time:
    value: "竞赛+投稿双周期；本轮只产规格+骨架，不跑真实实验"
    source: user
current_stage: S4          # S4 结果分析已交付（裁决=以现 framing 证据不足→S4 停点#2，待用户定框架路线）
stage_status:              # done | wip | blocked | backfilled | skipped
  S1: backfilled           # idea-card 由 SYNAPSE.tex 蒸馏，B1 撞车检索 2026-06-17 已做（含待核验残留）
  S2: done                 # 协议用户 06-20 认可 + 已被 S3 消费
  S3: done                 # 真实 API：signal(C1 KC-1 PASS) + m7 + CoQA + HotpotQA(bridge,CI)；evidence 轮缺口见 S4 caveats
  S4: done                 # 分析交付(claim 三档裁决+CI+M1-M11 回扫)；总裁决"不足以(现 framing)"→停点#2
last_handoff: "[S4 结果分析] — done — 2026-06-21"
